# Non-secret runtime config for the fighter-edge-app project. Firebase loads
# this file on deploy. Secrets (the OpenRouter key) never go here — they live
# in Secret Manager.
#
# Free-tier model, chosen 2026-09-19 after 8/8 passes through the real prompt
# and validator (scripts/ai-smoke.mjs). Free tier caps at 50 requests/day and
# 20/minute without purchased OpenRouter credits: right for development and a
# small beta, not for launch traffic. Delete this line to fall back to the
# paid DEFAULT_MODEL in src/openrouter.ts once the account is funded.
OPENROUTER_MODEL=deepseek/deepseek-v4-flash-0731:free
