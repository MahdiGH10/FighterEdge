# Copy to android/key.properties (git-ignored) and fill in. Never commit the
# real file or the keystore. CI uses the ANDROID_* environment variables
# instead (see .github/workflows/release.yml).
storeFile=/absolute/path/to/upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
